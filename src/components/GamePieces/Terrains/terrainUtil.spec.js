import {
    buildAllTerrains
} from './terrainUtil';

describe("terrainUtil", () => {
    describe("buildAllTerrains()", () => {
        it("should work", () => {
            let allTerrains = buildAllTerrains();
            // console.log(allTerrains);
        })
    })
})